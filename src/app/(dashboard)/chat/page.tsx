import AIChatBox from "@/components/AIChatBox";

type Props = {};
const ChatPage = ({}: Props) => {
  return (
    <div className="">
      <AIChatBox open={true} />
    </div>
  );
};

export default ChatPage;
